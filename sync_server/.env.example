# Skopiuj ten plik do .env i zmień wartości
# cp .env.example .env

# Tajny klucz API – min. 20 znaków, wszystkie komputery muszą mieć ten sam
API_KEY=wpisz-tutaj-swoje-tajne-haslo-minimum-20-znakow

# Poziom logowania: debug / info / warning / error
LOG_LEVEL=info
